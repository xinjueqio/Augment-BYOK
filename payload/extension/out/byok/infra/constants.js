"use strict";

const DEFAULT_UPSTREAM_TIMEOUT_MS = 120000;
const DEFAULT_SELF_TEST_TIMEOUT_MS = Math.min(60000, DEFAULT_UPSTREAM_TIMEOUT_MS);

module.exports = { DEFAULT_UPSTREAM_TIMEOUT_MS, DEFAULT_SELF_TEST_TIMEOUT_MS };
